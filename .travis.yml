dist: Bionic
sudo: require

language: 
  - cpp
compiler: 
  - g++

matrix:
  include:
    - os: linux
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - g++-10
            - gcc-10
        snaps:
          - name: cmake
            confinement: classic
            channel: latest/candidate
      env:
         - MATRIX_EVAL="CC=gcc-10 && CXX=g++-10"

before_install:
    - eval "${MATRIX_EVAL}"

script:
    - whereis g++
    - whereis cmake
    - sudo ln -s /usr/bin/gcc-10 /usr/local/bin/gcc
    - sudo ln -s /usr/bin/g++-10 /usr/local/bin/g++
    - sudo ln -s /usr/bin/cmake  /snap/bin/cmake
    - export CC=/usr/local/bin/gcc-10
    - export CXX=/usr/local/bin/g++-10
    - gcc -v && g++ -v && cmake --version
    - cd ${TRAVIS_BUILD_DIR}
    - ls
    - mkdir build
    - cd build
    - cmake -DCMAKE_BUILD_TYPE=RELEASE ../
    - make